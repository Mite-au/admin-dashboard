# Base URL of the NestJS backend (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Admin JWT used for server-side API calls (set in Vercel env vars for production)
ADMIN_API_TOKEN=
