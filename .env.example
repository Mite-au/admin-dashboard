# Base URL of the NestJS backend (no trailing slash).
# To run against the deployed alpha backend instead of a local one, see
# .env.alpha.local.example (cp it to .env.local).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
